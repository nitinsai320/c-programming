#include<stdio.h>
main()
{
  int n,rev,r,temp;
  scanf("%d",&n);
  rev=0;
  temp=n;
  while(n!=0)
  {
    r=n%10;
    rev=rev*10+r;
    n=n/10;
  }
    if(rev==temp)
    printf("it is palindrome");
    else
    printf("not palindrome");
}
