#include<stdio.h>
main  ()
{
	int i,n,c=0;
	printf("enter the number n\n");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		if(n%i==0)
		{
			c++;
		}
	}
	if (c==2)
	printf("It is  a prime number");
	else
	printf(" It not a prime number");
}
